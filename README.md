# Magic Task Board — Drag & Drop Task Board
*HTML5 Drag & Drop API · Vanilla JS · LocalStorage · CSS Variables*

“Магічна дошка для реальних справ: плануй з любов’ю, рухайся з фокусом, досягай із легкістю. Запрограмуй себе на успіх.”

Коротка візуальна дошка для задач з перетягуванням, пріоритетами, темною/світлою темою, редагуванням і збереженням у браузері.

![Vanilla JS](https://img.shields.io/badge/Vanilla%20JS-HTML5%20Drag%20%26%20Drop-blue)
![LocalStorage](https://img.shields.io/badge/Storage-localStorage-green)
![CSS Variables](https://img.shields.io/badge/CSS-Variables-orange)

## Зміст
- [Опис](#опис)
- [Що я опанувала](#що-я-опанувала)
- [Функціонал](#функціонал)
- [Як користуватись](#як-користуватись)
- [Технічні деталі](#технічні-деталі)
- [Технічний розбір](#технічний-розбір)
- [Запуск локально](#запуск-локально)
- [Структура проєкту](#структура-проєкту)

## Опис
**Magic Task Board** — це невелика SPA-дошка, де можна створювати картки, **перетягувати** їх між колонками *Start / Progress / Done*, задавати **пріоритет** (Low/Med/High), **редагувати/видаляти**, швидко **згортати/розгортати** довгі описи, **експортувати/імпортувати** дані у JSON та перемикати **темну/світлу тему**. Вся логіка працює повністю в браузері, без сервера.

“Реалізувала власний `HTML5 Drag&Drop` з позиціонуванням через `getBoundingClientRect()` і динамічний `drop-indicator`; обчислюю місце вставки в один прохід по `DOM`.”

“Повністю контролюю стан на клієнті: серіалізація `DOM → JSON`, `localStorage` з обробкою помилок, експорт/імпорт через `Blob/ObjectURL`.”

“UX-фічі: інлайнове редагування з автопідгонкою `textarea`, багаторядковий `preview` з виміром переповнення (`scrollHeight` проти `clientHeight`) і кнопкою ‘Показати більше/менше’.”

“Теми та налаштування збережені між сесіями; автосортування з перемикачем, щоб не конфліктувати з ручним `DnD`.”

“Без фреймворків, чистий JS + семантичний HTML + CSS змінні, що робить проєкт легким, зрозумілим і розширюваним.”

## Що я опанувала
- **HTML5 Drag & Drop API**: обробка `dragstart/dragover/drop` для карток і колонок; побудова індикатора позиції між елементами.  
- **Стан на клієнті**: збереження дошки в `localStorage` (створення/оновлення/очищення); імпорт/експорт JSON.  
- **UI-патерни**:
  - перемикач **темної/світлої теми** на CSS-змінних (`:root` і `:root.light`) + кнопка 🌗; стан теми також у `localStorage`;
  - **автосортування** карток за пріоритетом (High → Med → Low) з можливістю вимкнути/увімкнути;
  - **інлайнове редагування** через `textarea` (Ctrl/Cmd+Enter — зберегти, Esc — скасувати);
  - багаторядкове **обрізання тексту (line-clamp)** + кнопка **«Показати більше/менше»**;
  - **адаптивність** через `@media` (зручна робота на вузьких екранах).

## Функціонал
- **Створити задачу**: поле вводу + вибір пріоритету Low/Med/High → картка з’являється в колонці *Start*.
- **Перетягування (Drag & Drop)**: мишкою переміщайте картки між колонками та всередині колонки; видно **індикатор** вставки.
- **Сортування за пріоритетом**: опційне **автосортування** (кнопка «Автосортування: увімк/вимк»). Увімкнено — колонки впорядковуються High→Med→Low; вимкнено — картки залишаються там, куди їх перетягнули.
- **Редагувати/видаляти**:
  - редагування — подвійний клік по картці або кнопка ✏️;
  - видалення — кнопка 🗑️ з підтвердженням.
- **Кнопка «Показати більше/менше»**: довгі описи обрізаються до 4 рядків; натискання розгортає/згортає.
- **Темна/світла тема**: кнопка 🌗 перемикає, кольори беруться зі змінних у CSS; вибір зберігається.
- **Експорт/імпорт**:
  - **експорт** — кнопка «Експорт» зберігає `kanban-board.json`;
  - **імпорт** — «Імпорт» → виберіть JSON (є приклад у репозиторії).
- **Лічильники**: у кожній колонці показується кількість карток.
- **Адаптивний інтерфейс**: зручно на мобільних (малі екрани, більші клікабельні зони).

## Як користуватись
1. **Створення**: у хедері введіть назву задачі → виберіть **пріоритет** → натисніть **«Додати»**.
2. **Перетягування**: перетягніть картку мишкою в іншу позицію або колонку; орієнтуйтесь на **пунктирний індикатор**.
   - Якщо автосортування увімкнено, порядок одразу впорядкується за пріоритетами.
3. **Редагування/видалення**:
   - редагувати — ✏️ або подвійний клік;
   - видалити — 🗑️ (з підтвердженням).
4. **Пріоритет**: натисніть на бейдж пріоритету на картці, щоб циклічно змінити **Low → Med → High**.
5. **Довгі описи**: якщо текст обрізано — натисніть **«Показати більше»**; щоб згорнути — **«Показати менше»**.
6. **Тема**: кнопка **🌗** перемикає **Dark/Light**; вибір збережеться між перезапусками.
7. **Експорт/імпорт**:
   - **Експорт** — кнопка «Експорт» зберігає `kanban-board.json`;
   - **Імпорт** — «Імпорт» → виберіть JSON (наприклад, `kanban-board.json` з репо).
8. **Автосортування**: перемикач у хедері — **вимкнено** зберігає ручний порядок; **увімкнено** впорядковує за пріоритетом.

## Технічні деталі
- **Drag & Drop**: `dragstart`, `dragover` (з `preventDefault()`), `drop`; обчислення позиції вставки порівнянням `clientY` із центром карток.
- **Стан**: серіалізація в `localStorage` (`STORE_KEY`), імпорт/експорт JSON Blob-ом.
- **Пріоритети**: візуальні бейджі (Low/Med/High) + ранжування і сортування; перемикач автосортування (`localStorage`).
- **Теми**: перемикання класу `light` на `<html>` + змінні у `:root`/`:root.light`.
- **UX для довгих описів**: `-webkit-line-clamp` (4 рядки) + кнопка «Показати більше/менше».
- **Адаптивність**: `@media (max-width: 840px)` → одна колонка; центровані елементи на мобільних.

## Технічний розбір

### Архітектура
- **Vanilla JS + DOM API**, без фреймворків. Шаблон картки через `<template>`, маніпуляції DOM (`querySelector`, `classList`, `dataset`).
- **Стан на клієнті**: JSON у `localStorage` + синхронізація DOM ⇄ модель.
- **Прогресивні фічі**: тема, автосортування, “показати більше/менше” — опційні, конфіг збережений у `localStorage`.

### Модель даних
```js
// Item = { id: string, title: string, priority: 'low'|'med'|'high', createdAt: number }
const COLUMNS = ['Start','Progress','Done'];
const STORE_KEY = 'taskboard.board.v2';
```

### Drag & Drop (HTML5 DnD)
```js
// позиція вставки: шукаємо елемент "після якого"
function getDragAfterElement(container, y){
  const els = [...container.querySelectorAll('.item:not(.dragging)')];
  return els.reduce((closest, el)=>{
    const box = el.getBoundingClientRect();
    const offset = y - (box.top + box.height/2);
    return (offset < 0 && offset > closest.offset) ? {offset, element: el} : closest;
  }, {offset: Number.NEGATIVE_INFINITY}).element;
}

place.addEventListener('dragover', (e)=>{
  e.preventDefault(); // дозволяємо drop
  const after = getDragAfterElement(place, e.clientY);
  after ? place.insertBefore(dropIndicator, after) : place.appendChild(dropIndicator);
});
```

### “Показати більше/менше” + line‑clamp
```js
// перевіряємо, чи текст переповнений 4 рядки
function needsClamp(title){
  const was = title.classList.contains('clamp-4');
  title.classList.add('clamp-4');
  const over = title.scrollHeight > title.clientHeight + 1;
  if(!was) title.classList.remove('clamp-4');
  return over;
}

btnMore.addEventListener('click', (e)=>{
  e.preventDefault();
  const t = node.querySelector('.title'); // завжди "живий" вузол
  const expanded = btnMore.dataset.expanded === '1';
  t.classList.toggle('clamp-4', expanded);
  btnMore.textContent = expanded ? 'Показати більше' : 'Показати менше';
  btnMore.dataset.expanded = expanded ? '' : '1';
});
```

### Пріоритети та автосортування
```js
function priorityRank(p){ return p==='high'?0 : p==='med'?1 : 2; }
function sortPlace(place){
  const items = [...place.querySelectorAll('.item')]
    .sort((a,b)=> priorityRank(a.dataset.priority||'low') - priorityRank(b.dataset.priority||'low'));
  items.forEach(n => place.appendChild(n));
}
```

### Сховище, експорт/імпорт
```js
const Store = {
  load(){ return JSON.parse(localStorage.getItem(STORE_KEY) || '{"Start":[],"Progress":[],"Done":[]}'); },
  save(s){ localStorage.setItem(STORE_KEY, JSON.stringify(s)); }
};

// Експорт
const data = readFromDOM();
const blob = new Blob([JSON.stringify(data, null, 2)], {type:'application/json'});
const a = Object.assign(document.createElement('a'), { href: URL.createObjectURL(blob), download: 'kanban-board.json' });
a.click(); URL.revokeObjectURL(a.href);

// Імпорт
const file = input.files[0];
const imported = JSON.parse(await file.text());
renderState(imported); Store.save(imported);
```

### Тема (Dark/Light) на CSS-змінних
```js
const THEME_KEY = 'taskboard.theme';
function applyTheme(t){ document.documentElement.classList.toggle('light', t==='light'); }
themeBtn.addEventListener('click', ()=>{
  const next = document.documentElement.classList.contains('light') ? 'dark' : 'light';
  applyTheme(next); localStorage.setItem(THEME_KEY, next);
});
```
### UX для редагування
- Інлайнове редагування через `textarea`, авто-висота по `scrollHeight`.
- Гарячі клавіші: **Ctrl/Cmd+Enter** — зберегти; **Esc** — скасувати.
- Після редагування повторно рахується clamp і синхронізується стан.


## Запуск локально
> **Варіант 1 (простіше):**  
Відкрити `index.html` у браузері (Chrome/Edge/Safari/Firefox).

## Структура проєкту
```
.
├── index.html            # Розмітка, хедер з формою/кнопками, 3 колонки, шаблон картки
├── styles.css            # Темна/світла тема (CSS-змінні), стилі карток, адаптив
├── app.js                # CRUD, DnD, пріоритети, line-clamp, експорт/імпорт, тема
└── kanban-board.json     # Приклад експортованих даних
```

## Демо
### Desktop
![Demo](./demo/demo.gif)

### Mobile (responsive)
![Demo](./demo/demo_media.gif)








